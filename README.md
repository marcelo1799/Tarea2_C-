# Tarea2_C-

## Compilación e Instalación

Sigue estos pasos para compilar e instalar el proyecto:

1. Clona el repositorio:
   ```bash
   git clone https://github.com/tu-usuario/tu-repositorio.git
   cd tu-repositorio

   Instala las herramientas de desarrollo esenciales y CMake:

bash
Copiar código
sudo apt install build-essential cmake
Instala Qt y las herramientas de desarrollo:

bash
Copiar código
sudo apt install qtbase5-dev qt5-qmake
Instala Git si aún no lo tienes:

bash
Copiar código
sudo apt install git
Compilación e Instalación
Sigue estos pasos para compilar e instalar el proyecto:

Clona el repositorio:

bash
Copiar código
git clone https://github.com/tu-usuario/tu-repositorio.git
cd tu-repositorio
Crea un directorio de compilación y navega a él:

mkdir build
cd build
Ejecuta CMake para generar los archivos de compilación:


cmake ..
Compila el proyecto:


make
Ejecuta el programa:


./nombre-del-ejecutable
